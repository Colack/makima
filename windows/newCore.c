#include "includes.h"

#include "lib/hash.h"
#include "lib/util.h"
#include "lib/makima.h"
#include "lib/error.h"

int main(bool hashEnabled, bool debugEnabled) {
    if (hashEnabled) {
        char 
    }
}